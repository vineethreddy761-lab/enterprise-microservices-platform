apiVersion: apps/v1
kind: Deployment
metadata:
  name: enterprise-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: enterprise-redis
  template:
    metadata:
      labels:
        app: enterprise-redis
    spec:
      containers:
      - name: redis
        image: redis:7-alpine
        ports:
        - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: enterprise-redis-svc
spec:
  selector:
    app: enterprise-redis
  ports:
  - port: 6379
    targetPort: 6379
